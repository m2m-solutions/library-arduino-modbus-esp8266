#pragma once
#include "common.h"

Modbus::ResultCode handleFile(Modbus::FunctionCode func, uint16_t fileNum, uint16_t recNumber, uint16_t recLength, uint8_t* frame) {
    memset(frame, 0xFF, recLength);
}

initFile() {
    master
    slave
}
